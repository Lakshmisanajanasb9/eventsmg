# eventsmg

Setup :
    `python -m venv venv`
    `source venv/bin/activate`
    `pip install -r requirements.txt`
    
To run:
    `gunicorn run:app`

- [ ] payment
- [ ] categories filter and search bar for location
- [ ] create event
